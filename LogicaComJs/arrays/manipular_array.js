const numeros = [1, 5, 10, 20];

// numeros.push(30); // Acrescenta um ou mais elementos no final do array
numeros.pop(); // Removve o último elemento do array

numeros.unshift(500); // Adiciona um elemento no inicio do array
// numeros.shift(); // Remove o primeiro elemento do array

console.log(numeros);
